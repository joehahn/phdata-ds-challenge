.venv/
.ipynb_checkpoints/
__pycache__/
.DS_Store

#phData's own challenge materials, not mine to publish
phdata-ds-project-challenge.pdf
phdata-ds-project-challenge.zip

#the closing slide of the technical session, held back until then
architecture_slide.pptx

#the submission archive duplicates everything already in this repo
phdata_challenge_joe_hahn.zip

#generated by the notebooks, no reason to ship them
train.parquet
test.parquet
models.joblib

#phData's data, and nobody needs it to read the notebooks
project_data.csv
